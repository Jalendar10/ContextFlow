import React from 'react';
export default function InterviewProfile({value={},onChange,disabled}){
 return <details className="meeting-context" open><summary>Interview information</summary>{value.detectedStage&&<p role="status"><strong>Identified stage: {value.detectedStage}</strong><br/>{value.stageReason}</p>}<label>Interview email / invitation details<textarea aria-label="Interview email details" maxLength={10000} disabled={disabled} value={value.emailDetails||''} placeholder="Paste the recruiter’s email, interview agenda and instructions…" onChange={e=>onChange({...value,emailDetails:e.target.value,detectedStage:'',stageReason:''})}/></label><label>Known expectations / extra interview information<textarea aria-label="Interviewer expectations" maxLength={4000} disabled={disabled} value={value.expectations||''} placeholder="Expected topics, interview format, answer length or evaluation criteria…" onChange={e=>onChange({...value,expectations:e.target.value})}/></label><small>Paste the invitation above. Generate interview skills & answer guide to identify the current stage from the email, JD and résumé. Unclear stages are marked as unknown.</small></details>;
}
